---
worker: atlas
cycle: wave-fixing-2 cycle 2 (re-deploy 774f734 + Phanes graphviz dep)
stamp: 20260513-0344
status: shipped clean
ferry_triggered: false
medium_concerns_count: 5
high_concerns_count: 0
---

# Atlas Wave-Fixing #2 Cycle 2 Uncertainty Journal

## Confidence: HIGH on ship deliverable

All 10 ship criteria satisfied (build + push manifest f12322b5, K8s rollout gen 7 1/1 Ready, smoke 3/3 PASS, body grep 5/5 PASS, 5 resident chat real LLM, Pandora real V4-Pro drafts write, /api/diagram/demo 157 nodes Phanes endpoint live, rollback path operative gen 6 RS preserved). Proceed Manager hand-back without ferry.

## Medium concerns

### U-Atlas-WF2-cycle2-01: Pandora POST /api/refactor/simulate client timeout 90s

curl client cut at 90s (--max-time 90) waiting for full V4-Pro response body. Server-side dispatch CONFIRMED via pod filesystem inspect (`kubectl exec ls -la drafts/`) showing fresh sim folder `add-smoke-test-for-wave-fixing-dispatch-verify-b83eb7` written at 21:01 with diff.patch + src + tests subfolder. Pandora real V4-Pro generation took >90s to stream complete payload.

**Impact**: Demo Day 2 Manager Pan rehearsal must budget 90-120s wait time for refactor simulate response, OR pre-cache canned simulation via Triton semantic cache. Hafiz throwaway $5 budget acceptable for one live demo trigger but pre-cache safer.

**Mitigation already in place**: PRD Section 18.3 V4-Pro routing for Athena Refactor proposal author + Triton semantic cache cosine 0.85 + canned response pre-cache (D-Atlas-21 carry-forward). Demo can use the freshly-generated `add-smoke-test-for-wave-fixing-dispatch-verify-b83eb7` as a warm cache hit if user_intent text is similar.

**Decision**: Document as known Demo Day operational note for Pan; no Atlas code action needed. Triton circuit_breaker 5-fail 60s cooldown remains operative if cluster surface starts surfacing repeated timeout.

### U-Atlas-WF2-cycle2-02: image size delta +180MB build / +30MB runtime

Phanes graphviz triple-layer install added: backend-builder +180MB (graphviz + graphviz-dev + gcc + g++ + libc6-dev + python3-dev + pkg-config). Runtime stage discards builder layers so net runtime image grew only ~30MB (graphviz binary + Bitstream Vera fonts pulled in by graphviz Debian package).

**Impact**: Image pull at pod cold-start ~3-5s longer. Acceptable per Atlas-to-Production contract budget.

**Long-tail mitigation**: post-hackathon, swap eralchemy2 (pulls pygraphviz transitive) for pure-Python ERD alternative OR strip pygraphviz with constraint `pygraphviz; python_version<'0'` and let eralchemy2 fall back to graphviz-only render path. Out of hackathon scope.

### U-Atlas-WF2-cycle2-03: /api/llm/health calls_recorded baseline 0 at fresh pod start

Post-rollout new pod 7b86dd5d8b-6rl6f had calls_recorded=0 + total_cost_usd=0 at smoke start (in-memory counter reset on pod restart per Triton design). After 5 resident chat smoke + Pandora simulate dispatch the counter showed 2 + cost 0.0003 USD. The 2 + 0.0003 figure represents the cache-miss live LLM calls (3 of 5 resident chats were canned response cache hits; only 2 hit DeepSeek API live).

**Impact**: Demeter `llm_call_log` table is the persistent source of truth; in-memory counter is observability convenience only. Real demo cost tracking should query Demeter aggregator, not /api/llm/health pure counter.

**Decision**: Not blocking. Pan demo rehearsal Day 2 will trigger fresh dispatch which always shows live LLM cost increment in /api/llm/health for the demo session.

### U-Atlas-WF2-cycle2-04: Traefik default cert self-signed carry-forward Day 2

Same as D-Atlas-25 cycle 1 Wave-Fixing. HTTP 000 BYTES 0 without -k flag. HTTP 200 BYTES present with -k flag. Refactory infra team owns TLS cert lifecycle; Atlas scope is application Ingress YAML which already declares correct host + path routing.

**Impact Day 2 demo**: browser must "Advanced -> Proceed" through self-signed warning OR Hafiz Day 2 morning request Refactory team to install real cert. Pan rehearsal note must capture this.

**No Atlas action**: ingress.yaml unchanged from cycle 1 baseline.

### U-Atlas-WF2-cycle2-05: ghcr.io package visibility still "private" semantic

Manager Wave-Fixing #2 cycle 1 noted "admin:packages access via Manager via Web UI workaround". Image push still uses the GHCR_TOKEN write:packages scope; package visibility flag on ghcr.io web UI may still be "Private" requiring K8s `imagePullSecrets: ghcr-pull` (deployment.yaml line 54) referring to `ghcr-pull` Secret created via populate-secrets.sh.

**Verified live**: new pod 7b86dd5d8b-6rl6f Status Running with ImagePullSecret `ghcr-pull` working correctly (no ErrImagePull / ImagePullBackOff). Means `ghcr-pull` Secret is populated + token authorized for read:packages.

**Risk**: if user/Manager rotates GHCR_TOKEN, populate-secrets.sh must re-run + K8s Secret patched OR pod will fail pull next time. Document carry-forward to Pan Day 2 ops checklist.

## High concerns: none

No ferry trigger. All ship criteria satisfied.

## Decisions carried forward

- D-Atlas-22 INSECURE_TLS=1 smoke test flag baseline operative
- D-Atlas-25 self-signed Traefik cert browser "Advanced + Proceed" demo flow
- D-Atlas-26 RollingUpdate maxSurge=1 maxUnavailable=0 zero downtime confirmed cycle 2
- D-Atlas-WF2-01 graphviz triple-layer install committed to Dockerfile permanent
- D-Atlas-WF2-02 manifest f12322b5 active in cluster
- D-Atlas-WF2-05 rollback path gen 6 RS 5767f8c8d5 preserved

## Files touched this cycle

**Modified** (1):
- `infra/docker/Dockerfile` (graphviz + graphviz-dev + gcc + g++ + libc6-dev + python3-dev + pkg-config in backend-builder stage; graphviz in runtime stage)

**New artifacts** (4):
- `_meta/decision_log/atlas.md` (append D-Atlas-WF2-01 through D-Atlas-WF2-05)
- `_meta/uncertainty/atlas-wf2-cycle2-20260513-0344.md` (this file)
- `_meta/checkpoints/atlas-wf2-cycle2.md` (next)
- `_meta/handoff_log/wave-fixing-2_atlas_to_eunomia-rescue_20260513-0344.md` (next)
- `_meta/orchestration_log/V5_atlas_wave_fixing_2_locked_20260513-0344.md` (V5 snapshot)

**Touched (Docker image only)**:
- ghcr.io/finerium/codeplexrefactory:latest digest 8e10c839 -> f12322b5
- ghcr.io/finerium/codeplexrefactory:774f734 new tag

No source code change in `backend/app/*` or `frontend/*` (anti-collision matrix respected).
