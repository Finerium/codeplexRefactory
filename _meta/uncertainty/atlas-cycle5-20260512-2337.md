---
worker: atlas
wave: 3
cycle: 5 (covers cycle 2-4 continuation session)
authored: 2026-05-12 23:37 WIB Day 1 evening (date +%Y%m%d-%H%M = 20260512-2337)
confidence: medium-high (3x consecutive smoke PASS live; medium concerns below)
---

# Atlas Wave 3 Uncertainty Journal — Cycle 2-4 Continuation

## Context

Continuation session after cycle 1 stub ship. 4 mandatory artifacts cycle 1 already in place. This journal covers medium concerns from the cycle 2 image build + cycle 3 K8s deploy + cycle 4 smoke test E2E loop.

## U-Atlas-cycle5-01: ghcr.io package visibility private (PAT scope shortfall)

**Concern**: User's GHCR_TOKEN has scope `write:packages + read:packages + repo` — sufficient to push but NOT to flip package visibility to public (requires `admin:packages`). Mitigation was K8s imagePullSecret `ghcr-pull` injecting the PAT into the cluster for pull authentication.

**Downstream risk**: When PAT expires (30-day expiration per spawn directive), pod restart will fail to pull. Pan Day 2 demo rehearsal should manually verify the pull works pre-stage. Long-term: pre-Wave 3 Aletheia audit should escalate "package public visibility" to Ghaisan for `admin:packages` scope grant, or document the imagePullSecret rotation procedure.

**Action**: documented in handoff_log; no ferry. PAT lifetime 30 days >> hackathon Day 2 window.

## U-Atlas-cycle5-02: TLS cert at Refactory ingress is Traefik default self-signed

**Concern**: `curl -v` revealed `subject: CN=TRAEFIK DEFAULT CERT`. Browser will show invalid-cert warning on Day 2 demo. Smoke test passes with `INSECURE_TLS=1` skip-verify.

**Downstream risk**: Pan Day 2 demo browser flow needs either (a) Refactory cert-manager rotation to a Let's Encrypt cert OR (b) demo-time workaround (Chrome --allow-insecure ... flag, or curl-based demo). Atlas escalation deferred to Pan post-Wave 3 polish phase.

**Action**: Pan rehearsal Day 2 morning verify browser flow. If still self-signed, ferry to Ghaisan to request manual rotation from Refactory contact (panitia ops).

## U-Atlas-cycle5-03: Cross-worker dep contract gap (Triton's deps not in Hades pyproject.toml)

**Concern**: `openai` + `numpy` were imported by Triton's code but never declared in `backend/pyproject.toml` (Hades baseline). Caught only by container startup `ModuleNotFoundError`. Cycle 3 added both. Indicates broader gap: each worker authored `app/<their-domain>/` modules but did NOT update the shared pyproject.toml.

**Downstream risk**: Other Wave 3 workers may have similar deps missed. Aletheia final audit MUST run `import` smoke (`python -c "from app.main import app"` in fresh container) to catch any remaining ModuleNotFoundError. Atlas already added the 2 known.

**Action**: handoff_log flags "pyproject.toml contract integrity" as audit must-check for Aletheia. Possible additional missed deps if Aletheia audit surfaces issues.

## U-Atlas-cycle5-04: Parser `/api/parser/parse-repo` endpoint not E2E-smokeable via HTTP

**Concern**: Parser API requires server-side filesystem path (`repo_root`) which is not provisioned in the container (no demo repo volume mounted). E2E smoke step 5 substituted to `GET /api/llm/health` (D-Atlas-22) which still exercises api_router chain.

**Downstream risk**: Pan Day 2 browser demo "click a building → see parsed data" path needs either (a) a mounted demo repo volume (Pandora's `drafts/` may include one) OR (b) the in-container city dataset (Daedalus seeded fixtures) is used instead. Atlas defers to Pan demo rehearsal verification.

**Action**: handoff_log flags this as Pan must-rehearse. If parsed-data demo doesn't work, fallback to city visual + dashboard data only (still demonstrates the deploy + integration).

## U-Atlas-cycle5-05: K8s namespace `duopoly` permissions scoped — no events/nodes visibility

**Concern**: ServiceAccount `duopoly-sa` cannot `list events` or `get nodes` — diagnostic limitation if pod issues arise on Day 2. Atlas worked around by using `kubectl get pod -o json` for container status (which IS namespace-scoped readable).

**Downstream risk**: If pod crashloops in middle of demo with no obvious symptom, debugging will rely on `kubectl logs <pod> --previous` only. No event reason, no node-level forensics.

**Action**: Pan rehearsal needs a logs-only debug runbook. Atlas documented in handoff_log.

## U-Atlas-cycle5-06: Multi-arch buildx push has variable upload time + occasional GHCR auth timeout

**Concern**: First multi-arch push took ~370s for layer upload. Second push had a `Post "https://ghcr.io/token": read tcp ... operation timed out` error after 512s; retry succeeded. Network at user's location is intermittent.

**Downstream risk**: Pan Day 2 if image needs re-roll, push could fail mid-upload. Mitigation: image is cached in the cluster after first successful pull; updating tag `latest` while pod is running is sufficient via `kubectl rollout restart`. Image rebuild only required if backend code or pyproject.toml changes.

**Action**: pre-Day-2 Pan rehearsal verify `kubectl rollout restart` works without re-build. handoff_log documents this.

## Summary

6 medium concerns. NONE qualify ferry HIGH bar per spawn directive:
1. ghcr.io 401 → resolved via imagePullSecret (not ferry trigger).
2. kubectl connection refused → never occurred, full access.
3. Pod not Ready after 5 min → ROOT CAUSED and FIXED (D-Atlas-18, 19, 20, 21); never persisted beyond fix cycle.
4. Smoke test 3x ada 1 trial fail → 3x ALL PASS first run; no retry.
5. Cert self-signed → known issue, Pan demo concern not Atlas ship concern.

Atlas cycle 2-4 ship-clean. Smoke E2E 3x consecutive PASS satisfies SC-04 LOCKED Aletheia verify.
