## Uncertainty Journal: Pandora Wave-Fixing #3 Final (STAMP=20260513-0631)

**Worker**: Pandora
**Wave**: Wave-Fixing #3 (Manager #3 cluster final)
**Scope**: R-1 RECURRING root-cause fix + end-to-end Refactor chain verification

### Confidence-based action

**Verdict R-1 RECURRING: PASS** with HIGH confidence after real-backend curl + accept-endpoint smoke.

### Resolved

1. **R-1 RECURRING root cause was first-byte SSE latency** (not router wiring). Manager #2 cluster WF2-1 + WF2-3 correctly fixed router mount + DeepSeek dispatch + prefix double-mount, but tested via stub LLM. The real V4-Pro thinking-high Turn 0 takes 30-60 sec; until it returned, the SSE stream emitted zero frames. Browser fetch + curl both saw a stalled connection. Fix: emit `proposal.queued` SSE frame BEFORE the LLM dispatch.

2. **End-to-end chain is contract-clean**. Verified via real curl tests:
   - POST /api/refactor/propose -> 7 SSE frames over 149.7 sec
   - POST /api/refactor/simulate -> HTTP 202 + simulation_id
   - drafts/<sim>/ -> tests + impl files real-content (V4-Pro thinking high output)
   - GET /api/refactor/<sim>/accept-info -> 9347-byte diff ready
   - POST /api/refactor/<sim>/accept -> HTTP 200 + octet-stream diff

3. **AD-19 isolation property holds**. drafts/<sim>/ is the only write target. Production code SHA-256 unchanged across the entire simulate run.

### Open (low-confidence, deferred Cycle 2 polish)

1. **V4-Pro thinking-high latency at ~30-60 sec per turn = ~3-5 min total for 3-turn simulate**. The frontend `RefactorReviewVariant.runSimulation` POST returns HTTP 202 immediately; the user waits 3-5 min for `simulation_done` WS event. The mock pump fallback in `useSimulationEvents` covers Wave 2 mock mode but the real-mode user waits the full latency. Possible mitigations: (a) cut max_tokens for Turn 1 test_gen (currently 4000) to ~2000 since test files are small; (b) parallel Turn 1 + scaffold Turn 2 prep; (c) downgrade Turn 1 to V4-Flash thinking-medium to halve latency. Defer to Cycle 2 polish unless Ghaisan's pitch script needs faster beats.

2. **2 ghost buildings vs 3 ghost buildings**. Athena Turn 0 returned 2 ghosts for "add 2FA to login" (TwoFactorService + TwoFactorOtp) and 1 ghost for "add a logout button" (LogoutButton). PRD step 3 says "3 ghost buildings appear"; the real Athena adapts to intent complexity. Pitch flexibility: the PRD copy can read "up to 3 ghost buildings appear" without losing impact. No code change.

3. **Repo has no openspec/ folder** -> GitHub Issue fallback URL is the `openspec_change_path` value. This is correct per PRD Section 11 (progressive degradation). For demo footage, recommend Ghaisan run `openspec init` against the target demo repo OR clone `Finerium/codeplexRefactory` itself which presumably has openspec/ (but the running backend cwd is `backend/`, so the parent project openspec/ is at `../openspec/` not `./openspec/`). Quick check via `ls /Users/ghaisan/Documents/codeplexRefactory/openspec` from the running cwd would surface it; current /api/refactor/propose `repo_root` query param defaults to `.` (backend cwd). Cycle 2 polish: default repo_root to `..` so the parent project openspec/ is picked up.

4. **Frontend port mismatch**. Frontend dev runs on port 3000; backend on 8000. Next.js dev rewrites may be required (or absolute URLs). Currently `refactorClient.ts` uses relative URL `/api/refactor/propose` which assumes same-origin. For local dev test, the user must run `cd frontend && npm run dev` and ensure `next.config.js` rewrites `/api/*` to `http://localhost:8000/api/*`. Defer to Pan's demo-rehearsal step (not in Pandora's domain).

### Ferry decision

No ferry. R-1 verdict PASS is justified by live curl evidence + drafts/<sim>/ contents + diff download. The 3-5 min simulate latency concern is a Cycle 2 polish item, not a blocker.
