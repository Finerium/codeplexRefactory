# Atlas Cycle 1 Uncertainty Journal

**Worker**: Atlas
**Cycle**: 1 (stub)
**Timestamp**: 2026-05-12 21:37 WIB Day 1 evening
**Confidence level**: medium (proceed conservative per Atlas prompt ferry HIGH bar)

## Context

Cycle 1 ship intent: Dockerfile + 5 K8s manifest + 3 scripts + smoke test E2E offline-authoring. Docker tooling absent locally. kubectl absent locally. Authoring offline per Atlas agent prompt Section 7 Step 3 anticipated scenario.

## U-Atlas-01: Docker buildx + kubectl not installed on Ghaisan machine

**What I observed**: 
- `which docker` exit 127 not-found
- `which kubectl` exit 127 not-found
- `which buildx` exit 127 not-found
- `/Applications/Docker.app` not present
- `/opt/homebrew/bin/docker` + `/usr/local/bin/docker` not present

**Risk**: Cycle 2 image build + Cycle 3 deploy + Cycle 4 smoke test 3x all gated on Docker tooling availability. Single-point-of-failure for entire submission ship criteria SC-04.

**Mitigation**: 
- (a) Authoring offline Cycle 1 keeps critical path moving while Docker install is concurrent action by Ghaisan.
- (b) Atlas prompt explicitly anticipates this scenario: "lanjut Dockerfile + manifest authoring offline".
- (c) Ferry HIGH bar trigger if Cycle 2/3 still blocked > 6h cumulative wall-clock.

**Surface to V1 Orch**: Ghaisan needs to install Docker Desktop + kubectl Day 1 evening, OR rely on venue (Telkom Bandung lab) Day 2 having Docker + kubectl pre-installed. If neither available, ferry V1 Orch + drop protocol activate (pre-built canned fallback URL per Atlas prompt Section "Effort budget" Closing).

**Confidence**: medium. proceed conservative offline authoring. Ferry if Cycle 2 image build cannot start within Day 1 evening.

## U-Atlas-02: Frontend lockfile is npm not pnpm (contract template mismatch)

**What I observed**: 
- `frontend/package-lock.json` present (npm)
- `frontend/pnpm-lock.yaml` not present
- Wave 1+2 worker (Hera Cycle 5) used `npm install --legacy-peer-deps`
- Pythia contract `atlas-to-production.md` line 25-27 specifies `corepack enable pnpm && pnpm install --frozen-lockfile`

**Risk**: contract template authored before lockfile reality settled. If I follow contract verbatim Dockerfile will fail at frontend build (no pnpm-lock.yaml).

**Mitigation**: substitute `npm ci --legacy-peer-deps` for `pnpm install --frozen-lockfile`. Document substitution in decision log D-Atlas-02 + uncertainty journal here. Pythia contract semantic intent (deterministic frozen lockfile install) preserved with npm equivalent.

**Surface to Aletheia + Pythia**: contract template should be aligned to npm in next iteration if Wave 4 spawned. For Wave 3 ship, npm substitution is contract semantic-equivalent.

**Confidence**: high. npm ci --legacy-peer-deps is direct equivalent + tested by Wave 1+2 ship.

## U-Atlas-03: Next.js next.config.ts requires output: 'standalone' patch (cross-domain touch)

**What I observed**: 
- `frontend/next.config.ts` line 16-37 has no `output` field
- Pythia contract Dockerfile line 58 `COPY --from=frontend-builder /build/frontend/.next/standalone` depends on standalone build
- Cross-domain: next.config.ts owned by Calliope/Hestia Wave 1 visual layer per Pythia contracts

**Risk**: Lock 3 (no silent scope narrow) + Lock 5 (honest claim) potential violation if Atlas modifies frontend config silently.

**Mitigation**: 
- (a) Patch is minimal (single line `output: 'standalone'` added to nextConfig object)
- (b) Patch is infra-required for Atlas deploy domain (Dockerfile depends on standalone build)
- (c) Documented in Atlas decision log D-Atlas-04 explicitly as "build-plumbing config touch, not visual change"
- (d) Auditor Aletheia verify this patch is the minimal infra-required change

**Confidence**: medium. proceed with patch + decision log documentation. Alternative ferry would add latency without proportionate isolation benefit (no frontend worker active Wave 3).

**Surface to Aletheia**: verify patch is single-line `output: 'standalone'` + no other config changes.

## U-Atlas-04: SESSION_SECRET stub default vs production rotation

**What I observed**: 
- `backend/app/config.py` line 70 has `SESSION_SECRET` with default `"hades-cycle1-default-rotate-before-prod-deploy-7c4e2b9f6a1d3e8c"` 
- `.env` does NOT have `SESSION_SECRET=` populated
- Pythia contract atlas-to-production.md line 337-339 specifies `duopoly-session` K8s Secret with `secret` key

**Risk**: production deploy with default SESSION_SECRET = JWT cookie signing key is in source code which is insecure. Demo scope acceptable but PRD Section 19 security expectation may flag.

**Mitigation**: populate-secrets.sh generates fresh 32-byte urlsafe SESSION_SECRET at deploy time if not in .env. Idempotent rotation possible by `kubectl delete secret duopoly-session && ./populate-secrets.sh`.

**Surface to Aletheia + Demeter**: SESSION_SECRET rotation discipline. Recommend Ghaisan add `SESSION_SECRET=` to .env Day 1 evening with fresh `python3 -c "import secrets; print(secrets.token_urlsafe(32))"` output.

**Confidence**: medium. proceed with generate-at-deploy mitigation. Document in decision log D-Atlas-06.

## U-Atlas-05: Multi-arch buildx availability on Ghaisan machine

**What I observed**: 
- Docker absent locally so buildx availability unknown
- Multi-arch ARM64+AMD64 requires `docker buildx` with QEMU emulation

**Risk**: if Ghaisan installs Docker Desktop fresh, buildx + QEMU should be default. If lab venue uses CLI-only Docker engine, buildx may need explicit setup.

**Mitigation**: deploy.sh checks `docker buildx version` exit 0 at first step. If absent, falls back to single-arch native build via `docker build --platform $TARGETPLATFORM`. AMD64 single-arch likely sufficient because Refactory cluster nodes are x86_64.

**Surface to V1 Orch**: ferry if multi-arch fail + > 30 min debug (per Atlas prompt ferry HIGH bar Condition 1).

**Confidence**: medium. multi-arch is preferred + nice-to-have not essential. AMD64-only deploy still satisfies SC-04 + ship criteria.

## U-Atlas-06: Refactory K8s cluster public vs VPN-only access

**What I observed**: 
- `~/.kube/duopoly-config` exists at mode 600
- Cluster server `103.185.52.45:6443` per sourceoftruth Section 3.3
- Sourceoftruth Section 3.3 line 147 "Kalau timeout/error connection = bisa jadi VPN-only access dari venue Telkom WiFi (defer test ke hari-H di venue)"

**Risk**: kubectl deploy Cycle 3 + smoke test Cycle 4 may require venue WiFi access (Day 2 jam 11-13 submission window). Cycle 3 deploy from Ghaisan home WiFi may fail with connection timeout.

**Mitigation**: 
- (a) Defer kubectl exec to Day 2 venue per sourceoftruth advisory
- (b) Cycle 1 authoring offline allows Cycle 2 image build + image push to ghcr.io from Ghaisan home WiFi (registry pull from K8s cluster at venue)
- (c) Cycle 3 deploy + Cycle 4 smoke test run from venue lab Day 2 jam ~04:45-11:00

**Surface to V1 Orch**: confirm venue lab WiFi + Day 2 timing budget. Atlas Cycle 3+4 wall-clock ~2h target from venue.

**Confidence**: high. defer + Day 2 venue execution is per design.

## U-Atlas-07: Wave 3 stub-and-sync inter-worker dependency

**What I observed**: 
- Hades cycle 1 stub: API signature locked + stub response callable (pending ship)
- Triton cycle 1 stub: LLM client signature + stub response per resident (pending ship)
- Nemesis cycle 1 stub: 5 Apollo + 5 spec-drift detector signature (pending ship)
- Pandora cycle 1 stub: proposal author signature + drafts/ isolation test (pending ship)
- Demeter cycle 1 stub: SQLAlchemy schema + Alembic migration + stub query (pending ship)

**Risk**: Atlas Cycle 2 image build needs all Wave 3 worker cycle 2 full ship outputs. Cycle 1 stub from 5 backend workers acceptable for Atlas Cycle 1 stub Dockerfile authoring (only structural reference) but Cycle 2 image build needs full ship.

**Mitigation**: 
- (a) Cycle 1 authoring uses contract schema as reference, not concrete worker output
- (b) Cycle 2 image build await all 5 worker cycle 2 full ship
- (c) Dockerfile builds from `backend/` + `frontend/` whatever state at time of build, no Atlas-specific worker integration needed beyond directory copy

**Surface to Manager Wave 3**: sync event reporting Atlas Cycle 2 readiness gate on 5 backend worker cycle 2 ship.

**Confidence**: high. dependency is structural (filesystem layout) not interface.

## U-Atlas-08: Sourcing .env in populate-secrets.sh edge cases

**What I observed**: 
- `.env` at project root mode 600 gitignored
- DATABASE_URL has URL-encoded special chars `%2A`, `%3E`, `%7B`, `%3F`
- Shell `source .env` may have issues with `>` in unquoted value

**Risk**: populate-secrets.sh `source .env` may misparse DATABASE_URL value due to special chars.

**Mitigation**: use `set -a; source .env; set +a` pattern OR parse with `env $(grep -v '^#' .env | xargs)` to avoid shell interpretation. Pin pattern at script author time + test by `echo $DATABASE_URL` after source.

**Surface to Aletheia**: verify smoke test populate-secrets.sh source pattern correctly handles URL-encoded characters.

**Confidence**: high. shell sourcing patterns are well-established.

## Decision: proceed Cycle 1 offline authoring

Per Atlas prompt Section 7 Step 4 + Section "Closing" line 380 ("Kalau kubectl VPN-only fail di lab Ghaisan, defer cluster test ke venue hari-H, lanjut Dockerfile + manifest authoring offline").

Confidence: medium overall. 8 concerns logged. 0 ferry-trigger. Proceed.

---

(no further uncertainty entries this cycle, additional cycles author own uncertainty journal files with cycle number + fresh timestamp)
