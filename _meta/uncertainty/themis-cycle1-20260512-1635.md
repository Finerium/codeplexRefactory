# Themis Uncertainty Journal - Cycle 1

**Worker**: Themis
**Cycle**: 1 (Mandate 1 - Translate Metis md to 3 canonical _meta files)
**Date**: 2026-05-12 16:35 WIB
**Confidence**: medium on 2 items, high on rest

---

## U1: Git init + push akun Finerium scope ownership

**Concern**: Hephaestus handoff explicitly lists "Themis init git + first commit + push akun Finerium" as a Themis duty per "Metis Section 5.1 ship criteria". V1 Orch spawn directive does NOT include this in Mandate 1-3 listing nor in Section 19 "Yang lu create fresh".

**Confidence**: medium (Hephaestus had valid reason at handoff time; V1 Orch spawn may have intentionally scoped out or may have just missed the inclusion)

**Risk if I do it unsolicited**:
- Scope creep beyond spawn directive (Lock 3 expand violation)
- Modify project state outside Themis Mandate 1-3
- Burn Themis capacity budget on task not in scope (~10-15 min for git init + first commit + remote create + push)

**Risk if I skip**:
- Wave 1 Calliope / Hestia / Selene may need to git pull (no remote exists yet)
- Codebase not version-controlled until Wave 1 worker does it (worker pollution = drift from worker role)
- Aletheia Wave 3 final audit may flag missing git repo as ship-criterion violation

**Decision (per D4 decision log)**: defer. Surface to V1 Orch via ferry mechanism if needed.

**Proposed resolution**: V1 Orch decides one of:
- (a) Confirm Themis scope INCLUDES git init + push (amend spawn directive verbally next turn)
- (b) Confirm Themis scope EXCLUDES git init + push, defer to Pan post-Wave 3 OR Wave 1 worker setup
- (c) Themis does git init only (no push, no remote create) just to enable local commits, defer remote create to Atlas Wave 3 deploy phase
- (d) Themis does GitHub repo create + push but NOT OAuth app (separate concerns)

**Ferry threshold check**: Does this hit 1 of 5 HIGH bar triggers (critical block / contract conflict / anti-pattern violation / decision lewat scope / downstream cascade risk)?
- Critical block: NO (Wave 1 can proceed without git init for first 1-2 hours; workers can ferry if pull needed)
- Contract conflict: NO
- Anti-pattern violation: borderline (Lock 3 expand if I do unilaterally)
- Decision lewat scope: YES (this is exactly "Decision lewat scope domain" trigger)
- Downstream cascade risk: medium (Wave 1 + Wave 3 Atlas affected)

**Verdict**: 1 of 5 trigger met (scope decision). Recommend ferry but NOT critical urgency. Surface in handoff to V1 Orch + proceed conservative (skip git init this cycle).

## U2: GitHub OAuth app creation timing

**Concern**: Hephaestus handoff lists "Themis populate GitHub OAuth app credentials ke .env (CLIENT_ID + SECRET + WEBHOOK_SECRET)". V1 Orch spawn directive does NOT include this in Mandate 1-3 listing.

**Confidence**: medium

**Risk if I do it unsolicited**:
- Same as U1: scope creep beyond spawn directive
- Sensitive operation (modify `.env` mode 600 with real OAuth secrets)
- OAuth app creation requires visiting github.com/settings/applications/new manually (NOT pure CLI), requires Ghaisan browser auth

**Risk if I skip**:
- Wave 3 Hades real OAuth flow requires CLIENT_ID + SECRET + WEBHOOK_SECRET
- Atlas K8s Secret population step needs these for `duopoly-github-oauth` Secret
- If skipped, Wave 3 worker must do this mid-Wave 3 = blocking + sleep-cycle risk (Wave 3 start ~04:45 Day 2)

**Decision (per D4 decision log)**: defer. Surface to V1 Orch via ferry mechanism if needed.

**Proposed resolution**: V1 Orch decides one of:
- (a) Themis does OAuth app create now (Wave 0 has buffer)
- (b) Ghaisan does manually pre-Wave 1 (operator role 1: relay handoff; expand to 1+2 for this case)
- (c) Hades Wave 3 does it before real OAuth implementation (~04:45 Day 2; risk: requires browser interaction during sleep window)
- (d) Atlas Wave 3 does it during K8s Secret population step (~10:00 Day 2)

**Ferry threshold check**: 1 of 5 trigger met (scope decision lewat). Ferry recommended but not critical urgency.

## U3: Anti-collision matrix update for Themis 22-agent total

**Concern**: `_meta/contracts/_anti_collision_matrix.md` was authored by Pythia covering 19 worker pool + auditor pool + Pan. Themis added Wave 0 specialists (Pythia + Hephaestus + Themis) to roster.md totaling 22 agents.

**Confidence**: high (no actual collision risk; Wave 0 specialists are not re-used as Wave 1-3 worker names)

**Risk**: anti-collision matrix may be slightly inaccurate (says 19 prompts but roster has 22 agents). Auditor Eunomia Wave 1 may flag inconsistency.

**Mitigation in Themis roster.md**: explicit "Roster Summary" section notes "Worker prompt files: 19 (`.claude/agents/` populated by Hephaestus Wave 0, 15 worker + 3 auditor + 1 Pan; 3 Wave 0 specialists Pythia/Hephaestus/Themis have no `.claude/agents/` file since they spawn from `/orches-v1` directive)". This resolves the discrepancy.

**Verdict**: no ferry needed. Documented mitigation in roster.md is sufficient.

## U4: Hard handoff timestamp slip tolerance values

**Concern**: `_meta/wave_layout.md` adds "Slip tolerance" column with values like "up to 30 min" (Wave 0 to 1), "up to 1 hour" (Wave 1 to 2 and Wave 2 to 3). These values are Themis estimates derived from sum capacity buffer + Ghaisan/Hafiz sleep-cycle observation, NOT explicitly in Metis md.

**Confidence**: medium

**Risk if values are too tight**: V1 Orch ferries too eagerly when wave slips slightly, capacity burn
**Risk if values are too loose**: V1 Orch ferries too late when wave slips significantly, downstream cascade

**Mitigation**: tagged in `_meta/wave_layout.md` source citation that "Hard handoff timestamps" is Themis enrichment, not Metis verbatim. Auditor + V1 Orch may amend in `_meta/decisions/wave_slip_<N>.md` per Lock 9.

**Verdict**: no ferry needed. Documented as Themis estimate, open to V1 Orch override.

---

## Resolution path

For U1 + U2 (medium confidence ferry candidates): Themis includes both items in `_meta/handoff_log/wave0_themis_to_wave1.md` Cycle 8 output for V1 Orch decision. V1 Orch resolves before Wave 1 spawn or defers to specific Wave 1-3 worker.

For U3 + U4 (high confidence mitigated): documented in respective output files. No ferry needed.

---

**End of `_meta/uncertainty/themis-cycle1-20260512-1635.md`**.
