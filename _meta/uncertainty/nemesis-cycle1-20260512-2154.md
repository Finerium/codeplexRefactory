# Nemesis Cycle 1 Uncertainty Journal

**Authored**: 2026-05-12 21:54 WIB Day 1 evening (post Cycle 1 ship)
**Cycle**: 1 (stub ship)
**Lock 4 compliance**: 5 Apollo + 5 spec-drift pattern A-E LOCKED PRD Section 11. No Pattern F invented.
**Confidence baseline**: HIGH on schema + dispatcher + adapter pattern + 14/14 smoke. MEDIUM on items below.

## Medium concerns surfaced

### MC-01: 8-framework tree-sitter query for missing_auth detector (Phase B blind spot)

**Concern**: Phase B Topic 3c line 156 verbatim: "Multi-framework auth-route parsing (Express middleware vs. FastAPI dependency vs. Flask decorator vs. Rails before_action) requires per-framework query trees; no consolidated public query library exists."

**Risk**: Cycle 2 + 3 must author 8 framework queries (Express + FastAPI + Flask + Django + Gin + Echo + Spring + Actix). Each query needs validation against framework-specific fixture file. Risk of false positive on lambda routes, factory-pattern routing, dynamic decorator composition.

**Severity if blocks**: HIGH (missing_auth is 1 of 5 Apollo, plus Aletheia audit gate item).

**Mitigation planned**:
1. Allowlist per framework signature via imports check (already designed in drift algo decision doc).
2. Unknown framework fallback emits `info` severity finding labeled `missing_auth_framework_unknown` per Lock 5 honest claim, NOT silent pass.
3. Per-framework `.scm` query file at `backend/app/services/detectors/queries/missing_auth_<framework>.scm` lets future hackers patch one framework without touching others.

**Ferry threshold**: Per worker prompt HIGH bar trigger 1: "tree-sitter query per framework fail OR OSV API rate limit + greater-than 30 min debug". If single framework query takes greater-than 30 min, ferry V1 Orch.

**Status**: WATCH. Cycle 2 starts with Express + FastAPI + Flask + Django (4 framework). Cycle 3 adds Gin + Echo + Spring + Actix.

### MC-02: OSV API rate limit risk during full repo scan

**Concern**: NodeGoat fork has ~30+ npm dependencies in package.json. Each requires OSV API POST query. 30 sequential requests at OSV's ~1000 RPM hard cap is fine, but if multiple scans run in parallel during demo (e.g., Atlas K8s replicas), could hit rate limit.

**Severity if blocks**: MEDIUM (outdated_deps is 1 of 5 Apollo, but degraded fallback is fine for stub demo).

**Mitigation planned**:
1. `asyncio.Semaphore(10)` cap concurrent OSV calls per scan (drift algo decision doc Cycle 2 spec).
2. Exponential backoff retry 2s 4s 8s 3 attempts on 429.
3. After exhaustion, emit `info` severity finding labeled `osv_rate_limit_skipped` per Lock 5 (not silent pass).
4. In-memory cache per scan_run so package-version pair query memoized.

**Ferry threshold**: Per worker prompt HIGH bar trigger 1: greater-than 30 min OSV debug. Default = mitigation #3 emits info finding + dashboard shows it.

**Status**: WATCH. Cycle 2 implements with mitigations baked in.

### MC-03: Pattern E git history fallback when git unavailable

**Concern**: Pattern E (OpenSpec drift) requires `git log` subprocess to enumerate commits touching files referenced in archived spec proposals. If demo fixture or production K8s pod lacks git binary (e.g., minimal Alpine image), subprocess returns non-zero.

**Severity if blocks**: MEDIUM (Pattern E is 1 of 5 spec-drift, but Aletheia audit gate requires 5/5 trigger).

**Mitigation planned**:
1. Detect git availability via `shutil.which("git")` at startup.
2. If unavailable, emit `info` severity Pattern E event labeled `spec_e_git_unavailable` per Lock 5 (drift algo decision doc Pattern E mitigation step).
3. Atlas Dockerfile includes git binary in production image (cross-worker handoff to Atlas Cycle 2).

**Ferry threshold**: Only ferry if BOTH git unavailable AND Atlas refuses to add git binary. Default = mitigation #2.

**Status**: WATCH. Cycle 5 implements with fallback.

### MC-04: AST-diff inferred algo not validated empirically until Cycle 5

**Concern**: Phase B Topic 3c line 151 verbatim: "Algorithm sketch: parse OpenSpec requirement clauses, extract referenced symbols (function names, endpoints), confirm presence via tree-sitter parsed tree of current code; missing references = drift." Marked `[INFERRED]` because no community precedent.

**Risk**: Cycle 5 demo dataset may not trigger Pattern E AST-diff supplemental evidence cleanly if my regex for "extract referenced symbols" misses common markdown clause patterns (e.g., proposal.md uses tables instead of code blocks).

**Severity if blocks**: LOW for Pattern E primary trigger (opsx bypass detect via commit message regex is deterministic + robust). HIGH for AST-diff supplemental evidence if pitch demands it.

**Mitigation planned**:
1. Pattern E primary trigger = commit message opsx prefix check (deterministic, low risk).
2. AST-diff supplemental evidence = secondary enrichment (best-effort, falls back to empty `symbol_absent_drift: []`).
3. Cycle 5 testing on Themis Day-0 prep demo dataset validates regex against actual proposal.md format.

**Ferry threshold**: Only ferry if pitch explicitly demands AST-diff visualization AND fixture proposal format incompatible. Default = primary trigger alone passes Aletheia audit.

**Status**: WATCH. Cycle 5 implements.

### MC-05: Demeter persist_finding + persist_drift_event methods not yet defined on real Protocol

**Concern**: `app/services/demeter_service.py:76` `DemeterServiceProtocol` only declares `upsert_user` + `persist_pr_event`. Per `nemesis-to-demeter.md` Demeter Wave 3 owner adds `persist_finding` + `persist_drift_event` Cycle 2.

**Risk**: If Demeter Cycle 2 ships different signature than my adapter expects, integration breaks at runtime (not test time, because adapter falls back to buffer mode).

**Severity if blocks**: HIGH for production deploy, LOW for cycle 1 + 2 test demos (buffer mode is fine).

**Mitigation planned**:
1. Adapter uses `hasattr(real, "persist_finding")` duck-typing check before delegating; absence falls back to buffer-only path (no crash).
2. Authored `_meta/handoff_log/wave3_nemesis_to_demeter.md` reaffirming exact Pydantic schema + method signature so Demeter consumer signs off pre-impl.

**Ferry threshold**: Only ferry if Demeter ships breaking schema (e.g., snake_case vs camelCase or category enum mismatch).

**Status**: WATCH. Handoff log authored for cross-checking.

### MC-06: Adapter singleton state in pytest fixtures (test order dependence risk)

**Concern**: 4 adapters use module-level `_singleton` pattern. Without `reset_*_adapter()` autouse fixture, tests would accumulate state across runs (WS publish buffer grows, Demeter buffer grows). The autouse `_reset_adapters` fixture handles this in smoke test, but other test files (Hades, Triton, Pandora) may share unrelated singletons.

**Risk**: LOW. Tests in different files run in separate pytest collection groups. autouse fixture is per-test isolation. Verified 176/176 PASS post-cycle 1 ship.

**Severity if blocks**: LOW.

**Mitigation planned**: Already implemented via autouse fixture in `test_nemesis_detectors_smoke.py`. Cycle 2+ will add similar fixtures to integration test files.

**Status**: RESOLVED via autouse fixture. Monitor full suite re-run as cycles progress.

## High-confidence items (no concern)

- Pydantic schema verbatim Pythia contract: schema definitions cross-checked line-by-line against `nemesis-to-demeter.md` + `nemesis-to-asclepius.md` + `triton-to-nemesis.md`. 14/14 schema enum compliance test PASS.
- Dispatcher orchestration order: parse to Apollo parallel to Argus enrich to drift parallel to persist to publish. Matches Pythia contract `nemesis-to-demeter.md` line 109 consumer pattern verbatim.
- 4 adapter pattern with graceful fallback: 14/14 smoke + 176/176 full suite PASS confirms no regression.
- Anti-pattern locks compliance: no em dash, no emoji, no silent scope narrow (all 11 detector authored), no silent assume (this doc + drift algo doc), honest claim discipline ([STUB cycle-1] labels everywhere), Greek naming (Nemesis), no paid services (OSV free + DeepSeek $5 throwaway), 5 pattern A-E LOCKED + 5 Apollo LOCKED.

## Aletheia handoff note

Aletheia Wave 3 final audit verifies (per `aletheia-wave3-audit.md` line 64-77):
- 5 Apollo detector each fires on NodeGoat fork: cycle 5 verifies, cycle 1 stub trigger is mock-labeled.
- 5 spec-drift pattern A-E each fires on demo dataset: cycle 5 verifies, cycle 1 stub trigger is mock-labeled.
- Argus CVSS scoring + exploit pattern + mitigation: cycle 3 real Triton call, cycle 1 canned per category satisfies signature.
- WebSocket finding-events push: cycle 1 verified via 14/14 smoke, integration with Asclepius mock consume verified cycle 5.

## Locks compliance attest

- Lock 1 (no em dash): grep clean.
- Lock 2 (no emoji): grep clean.
- Lock 3 (no silent scope narrow): 11/11 detector authored, none dropped silently.
- Lock 4 (no silent assume): this journal + drift algo decision doc capture all assumptions.
- Lock 5 (honest claim discipline): every cycle 1 mock labeled [STUB cycle-1] inline + dispatcher reports `cycle="stub-cycle-1"`.
- Lock 6 (capacity): cycle 1 used ~22 min, well within 60-90 min budget.
- Lock 7 (Greek naming): Nemesis confirmed.
- Lock 8 (no paid services): OSV API free no-auth, DeepSeek throwaway.
- Lock 9 (V_n snapshot): cycle 1 produces V3_nemesis_cycle1_stub snapshot.
- Lock 10 (audit gate): Aletheia mandatory post Wave 3, audit checklist tracked in `aletheia-wave3-audit.md` consumed in plan.

No ferry triggered cycle 1. Default uncertainty journal + proceed conservative.
