# Uncertainty journal: Demeter cycle 1

**Stamp**: 2026-05-12 22:25 WIB
**Confidence**: medium-high (live Postgres validated, 13/13 live tests PASS)

## Medium concerns

### M-01 Pydantic camelCase alias generator deferred

Selene's TypeScript types expect camelCase JSON (per `selene-to-demeter.md` Asumption 1). Demeter ships snake_case Pydantic field names. FastAPI returns snake_case by default. Wave 1 Selene swap cycle 2 may need `alias_generator` if frontend cannot tolerate snake_case. Workaround: Selene `transformResponse` adapter, or add `Config.alias_generator` per model.

### M-02 Materialized view refresh strategy

Migrations use `WITH NO DATA` clause so initial `alembic upgrade head` succeeds on empty DB. Views populate via `refresh_dashboard_views()` / `refresh_activity_views()` on-demand. Wave 3 cycle 2 may add FastAPI BackgroundTasks scheduler per OQ-01. Currently dashboard query catches `UndefinedTableError` to handle pre-refresh queries gracefully.

### M-03 Session auth stub-tolerant for 1-click issue

`POST /api/findings/{finding_id}/to-issue` accepts `is_authenticated=False` stub session in non-production via `_allow_stub_session()`. Production hardening: Hades cycle 2 will raise 401 in `require_session` for missing/invalid JWT, making this branch unreachable. Demo flow stays unblocked.

### M-04 SQLAlchemy ORM `event_store.py` co-exists with raw asyncpg

ORM models in `app/models/event_store.py` define schema for Alembic autogenerate use (Wave 3 cycle 2 may run `alembic revision --autogenerate`). Runtime persistence uses raw asyncpg directly (not ORM session) for speed. No conflict; ORM purely declarative for migration schema source-of-truth.

### M-05 OpenSpec runtime untested at integration

`OpenSpecRuntime.list_specs()` etc. wrap subprocess but no integration test invokes the real `openspec` binary. Pythia listed openspec as Wave 0 deliverable. Manual smoke deferred to Wave 3 cycle 2 when Athena proposal author wires through.

## Low concerns

### L-01 Cost tracking drain not auto-scheduled

`CostTrackingService.drain_buffer_to_postgres()` ships as on-demand only. Auto-schedule (FastAPI BackgroundTasks every N seconds) deferred to Wave 3 cycle 2 polish.

### L-02 Sparkline + burndown computation simplified

Dashboard `RepoStatus.sparkline` returned empty; `BurndownPoint` list empty. Wave 1 Selene mock provides shape; Wave 3 cycle 2 can populate from `pr_events` GROUP BY day if rehearsal demands.

## No ferries triggered

All work scope completed within ferry HIGH bar. No schema break Selene/Boreas (snake_case noted M-01 above, not breaking). No directive plain-text token storage. No cascade risk.
