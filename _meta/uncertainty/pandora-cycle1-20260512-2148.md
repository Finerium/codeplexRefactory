# Uncertainty Journal: Pandora Cycle 1

**Worker**: Pandora (Wave 3)
**Cycle**: 1 (combined cycle 1-5 single ship)
**Timestamp**: 2026-05-12 21:48 WIB Day 1 evening
**Confidence**: Medium per Lock 6 (proceed with concerns logged); no Ferry triggers hit

## Concern 1: Ghost building position layout strategy (Cycle 2 polish candidate)

**Topic**: Pandora `proposal_author._layout_position(index)` places ghosts at `x = 68 + 10 * idx, z = -22` (the band Wave 2 mock used).

**Confidence**: medium (visual hand-off works for the demo, but is non-optimal for proposals that touch multiple districts).

**Risk**: A proposal that adds new files spanning multiple existing districts (e.g., "extract payment service from monolith" touching `frontend/`, `backend/auth/`, `backend/billing/`) gets all ghosts clustered at `x >= 68`, far from the original districts. Visually the proposal looks like "new district appearing on the right side" rather than "ghosts adjacent to affected files".

**Mitigation**: Wave 2 mock used the same strategy and Asclepius rendered it acceptably (the dual review gate is what carries the pitch beat, not pixel-perfect ghost placement). Cycle 2 polish: optional `_district_adjacent_placer(suggestedFilePath, iris_layout)` that computes ghost position by reading the Iris squarified treemap layout JSON + placing the ghost at the nearest unoccupied slot adjacent to the most-related existing building.

**Surface for Eunomia/Dike/Aletheia**: monitor demo rehearsal; if 14s mock pump feel "ghosts look orphan", trigger Cycle 2 polish.

## Concern 2: V4-Pro thinking high latency on Turn 1 + Turn 2 may exceed 14s total

**Topic**: Per `_meta/contracts/triton-to-pandora.md` Asumption 5 + open question OQ-07: V4-Pro thinking high latency is "5 to 15 seconds per turn" per Phase B estimates. Pandora's simulation engine schedules Turn 1 + Turn 2 at V4-Pro think high; Turn 3 at V4-Flash non-think.

**Confidence**: medium (Triton client + defensive layer not yet shipped; real latency unknown).

**Risk**: If Turn 1 + Turn 2 each take 15s, the total simulation is ~35s (including Turn 3 ~5s + overhead). Asclepius Wave 2 mock pump uses ~14s total; the visual progress indicator pacing was calibrated against the 14s budget. Real backend at 35s would feel "slow" vs the mock + may trigger the user to abort.

**Mitigation**: (a) Triton's defensive layer includes semantic cache cosine 0.85 threshold + canned response pre-cache for top-10 demo questions; the 2FA proposal pre-cache could collapse Turn 1+2 to canned-hit ~100ms each. (b) Optional Triton fallback Flash think high (per OQ-07 stretch decision) cuts latency by ~3x. (c) Asclepius's `SimulationProgressIndicator` interpolates progressPercent smoothly so 35s pacing still looks active.

**Surface for Eunomia/Dike/Aletheia**: bench V4-Pro think high p50 + p95 latency in Cycle 2 + Aletheia audit "DeepSeek per-resident routing" item. If p95 > 20s/turn consistently, ferry V1 Orch for Triton client tuning.

## Concern 3: OpenSpec change folder validates clean via `openspec validate <change-name>`

**Topic**: `.claude/agents/pandora.md` ship criteria includes "OpenSpec change folder validates clean (openspec validate <change-name>)". The current `openspec_generator.py` writes proposal.md + design.md + tasks.md but I have NOT run `openspec validate` against the output to confirm.

**Confidence**: medium ([UNVERIFIED] openspec CLI integration).

**Risk**: OpenSpec v1.0 validator may have stricter requirements (e.g., front-matter, specific heading order, required sections). The generated output is structurally sound but may fail validation on a missing-field check that I cannot inspect without running the CLI.

**Mitigation**: The OpenSpec validate skill is `cd ~/Documents/codeplexRefactory && openspec validate <change-name>`. If the CLI is available in the demo environment, Cycle 2 polish adds a `subprocess.run(["openspec", "validate", change_name])` post-generation hook with a parse of the output. If missing, defer to Aletheia audit + manual fix.

**Surface for Eunomia/Dike/Aletheia**: Aletheia audit gate item "OpenSpec validate clean (both folders)" verifies this at the audit step. If FAIL, re-spawn Pandora Cycle 2 polish per Lock 10.

## Concern 4: Triton + Hades + Demeter Cycle 1 stub timing

**Topic**: Per Wave 3 spawn directive, 6 workers spawn parallel. Pandora's stub-and-sync pattern means I do not block on Triton + Hades + Demeter Cycle 1 stubs; my in-house `StubLLMClient` + `StubDemeterAdapter` + `ParserServiceProtocol` carry the simulation engine end-to-end. Cycle 2 swap requires the upstream stubs to land.

**Confidence**: medium (depend on Manager Wave 3 sync events landing per STATUS.md).

**Risk**: If Triton's `DeepSeekClient` ships with a different method signature than my `LLMClientProtocol`, the Cycle 2 swap is more than a 1-line import edit + may require updating call sites. Same for Hades' `ParserService.find_callsites` and Demeter's `DemeterService.persist_*` methods.

**Mitigation**: My `Protocol` definitions are sourced verbatim from the Pythia contract files (`triton-to-pandora.md`, `hades-to-pandora.md`, `pandora-to-demeter.md`). Pythia contracts are LOCKED. If Triton / Hades / Demeter ship a different signature, that is a contract violation per Pandora ferry condition 2; I would ferry V1 Orch to coordinate. Most likely outcome: minor signature drift (e.g., parameter naming) requires a thin adapter wrapper.

**Surface for Eunomia/Dike/Aletheia**: Aletheia audit "Contract integrity" item verifies all 4 of my contracts match the actual upstream ship.

## Concern 5: Drafts/ directory cleanup on Accept default (cleanup=false)

**Topic**: `POST /api/refactor/{simulation_id}/accept` accepts an optional `?cleanup=true` query. Default is `cleanup=false`: the drafts directory is preserved after the diff download so the user can re-download if the first attempt fails.

**Confidence**: medium (no PRD directive on retention).

**Risk**: K8s pod restarts during the demo could lose the persistent drafts volume if Atlas does not mount it correctly. A `cleanup=false` default means orphan drafts accumulate over multiple simulations during dev/demo + potentially fill the volume. PRD Section 12.4 mentions "max 7 day retention as defensive cleanup" but Wave 3 does not yet have a cron / TTL.

**Mitigation**: (a) Atlas Wave 3 mounts `DRAFTS_ROOT` to a persistent volume per `infra/k8s/`. (b) Demo flow uses `?cleanup=true` on the Accept call per the Asclepius UI's DualReviewGate handler so the volume stays bounded. (c) Cycle 2 polish: add a startup hook that scans `drafts/*` and deletes directories older than 1 day.

**Surface for Eunomia/Dike/Aletheia**: Atlas audit on K8s volume mount; Aletheia audit on demo flow trial run.

## Concern 6: Background-task race with TestClient teardown (test-only)

**Topic**: `test_simulate_creates_drafts_dir_after_background_task` polls for `diff.patch` to appear because FastAPI `BackgroundTasks` fires after the response is fully read in `TestClient`. The poll has a 2-second wall-clock budget (40 * 50ms).

**Confidence**: medium (test fixture race depends on the asyncio scheduler).

**Risk**: On a slower CI runner (e.g., GitHub Actions free tier), the background task could take longer than 2 seconds + the poll would timeout. The test would flake.

**Mitigation**: 2 seconds is generous given the StubLLMClient returns canned responses in microseconds; the wall-clock cost is mostly the asyncio event loop overhead. If a flake occurs, the polling budget extends to 5s or the test mocks the background-task invocation to fire synchronously (per FastAPI documentation `app.dependency_overrides`).

**Surface for Eunomia/Dike/Aletheia**: monitor pytest flake rate; ferry if > 2 flakes per 100 runs.

## Concern 7: GitHub Issue fallback only generates a draft URL (no API create yet)

**Topic**: `github_issue_fallback.create_github_issue(draft)` returns `{"mode": "draft-url", "url": <pre-filled-new-issue-url>}` because Pandora Cycle 1 stub does not yet call Hades' PyGithub wrapper for actual API creation.

**Confidence**: high (this is the documented Cycle 1 stub behaviour per the function docstring + [STUB] label).

**Risk**: None at Cycle 1; the fallback URL works with the GitHub Issues UI directly. The `create_github_issue` function is labelled [STUB Cycle 1, real Hades PyGithub Cycle 2 full impl] per Lock 5.

**Mitigation**: Cycle 2 swap: import Hades' GitHub client + call `client.create_issue(repo_slug, title, body, labels)` when `github_client` is provided. Test fixture mocks `github_client` so the test does not hit real GitHub.

**Surface for Eunomia/Dike/Aletheia**: Aletheia audit Layer 1 hybrid write item; if Atlas deploys without write:issues OAuth scope, fallback URL is the demo path.

## Concern 8: Pydantic v2 strict mode does not error on extra keys (round-trip resilience)

**Topic**: `SimulationEvent.payload` is `SimulationEventPayload` with all-optional fields. If Asclepius TS counterpart adds a new field (e.g., `narration: string`), the Python round-trip ignores it silently.

**Confidence**: low (this is by-design tolerance for forward-compat).

**Risk**: If Asclepius adds a new field that Pandora needs to persist, Pandora silently drops it before writing to Demeter `simulation_events.payload JSONB`. The data is lost.

**Mitigation**: The `payload` field on `SimulationEventPersist` is `dict[str, Any]` (free-form JSONB). The simulation engine `_publish_stage` calls `payload.model_dump(mode="json")` which serialises ONLY declared fields. If Asclepius adds a field, the Pandora producer would not emit it; this is a one-way concern (TS extension > Python silently drops; Python extension > TS silently drops via `as RefactorEvent` typecast).

**Surface for Eunomia/Dike/Aletheia**: contract change requires coordinated TS + Python update in the same PR/cycle. Aletheia audit "Contract integrity" item verifies schema parity.

---

**No Ferry triggered**. All 5 high-bar conditions checked + ruled out:
1. Drafts isolation violation detected: NO (14 attack-vector tests pass; mtime sentinel test passes).
2. RefactorProposal / SimulationEvent schema break vs Asclepius: NO (parity test passes).
3. Directive to simulate against production code: NO (no such directive received).
4. OQ-09 Accept scope coordinate with Atlas: deferred to Atlas deploy + monitored via Concern 5 above (cleanup default).
5. Cascade risk: NO (stub-and-sync pattern isolates Pandora from upstream timing).

**Confidence-based action**: Medium concerns logged here; proceed cycle ship.
