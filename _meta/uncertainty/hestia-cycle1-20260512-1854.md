# Hestia uncertainty journal, Cycle 1

**Date**: 2026-05-12 18:54 WIB
**Confidence baseline**: high overall, three medium-confidence observations surfaced below.

## U1: Component path divergence (Hestia agent spec vs Ghaisan spawn directive)

**Confidence**: medium

**Observation**: the Hestia agent prompt at `.claude/agents/hestia.md` Section 3 Pythia output schema specifies `frontend/components/start/*`. The Ghaisan spawn directive in this session's first message specifies `frontend/components/entry/`. The two paths name the same domain.

**Decision**: followed Ghaisan spawn directive (`components/entry/`) as the operator-authoritative source. Agent spec was authored Wave 0 by Hephaestus; spawn directives override per the V1 Orch turn-by-turn pattern.

**Risk to downstream**: low. Eunomia Wave 1 audit reads handoff log + checkpoint to find file paths; both artifacts cite `components/entry/`. Hades Wave 3 only consumes the OAuth route handler at `frontend/app/api/auth/github/start/route.ts` which is path-unchanged. Future Wave 2 panels do not import from Hestia's components.

**Mitigation**: documented in this journal plus the checkpoint plus the handoff log so any cross-reader sees the path.

## U2: Daedalus + Calliope build conflicts at type-check + production-build stage

**Confidence**: medium (downstream observation, not a Hestia block)

**Observation**: during `npx tsc --noEmit` and `npx next build`, two sibling-worker outputs raised errors:

1. `src/components/marketing/TrinityArt.tsx` (Calliope Wave 1 landing) has TypeScript parser errors at lines 6 through 11, looks like an unterminated string literal cascade.
2. `components/dashboard/BurndownChart.tsx` (Selene Wave 1 dashboard) has a Recharts `Formatter<ValueType, string>` type-mismatch at line 123.
3. `next.config.ts` carries a custom webpack alias for the Three.js single-instance pattern but Next.js 16 default bundler is Turbopack. `next build` fails with a Worker error unless `--turbopack` flag passed. Even with the flag, the Selene type error blocks production build.

Hestia output is type-clean and renders cleanly under `npx next dev --turbopack`. Both 302 redirect smoke tests pass. SSR HTML weighs 107KB and contains every ship-criteria string verbatim.

**Risk to Hestia**: zero direct. The sibling errors do not touch any Hestia code path. Hestia routes load fine in dev mode.

**Risk to Wave 1 ship**: medium. If Eunomia audit gates on `next build` clean (rather than `next dev` clean), the wave cannot ship until Calliope + Selene + Daedalus fix their respective issues. Daedalus is currently in Cycle 4 ship; Selene + Calliope still in flight.

**Ferry recommendation**: not ferried, surface for Eunomia. V1 Orch awareness only. This is not a Hestia HIGH-bar trigger because no Hestia decision is needed to resolve it; the fixes belong to the workers who own those files.

**Mitigation**: dev-mode smoke test results captured in this journal plus the checkpoint, so the audit can see Hestia is independently ship-ready even if the wave aggregate fails until sibling fixes land.

## U3: Tweaks panel host integration not ported

**Confidence**: medium

**Observation**: the bundle ships `tweaks-panel.jsx` with a claude.ai/design host protocol (postMessage to `__edit_mode_set_keys`, drag-and-drop floating panel, etc). This is a claude.ai/design tooling layer, not part of the visitor-facing Entry page. The bundle's `entry-app.jsx` wired the Tweaks panel as a child of `App` so the operator could tune `warmth`, `doorStrength`, `shyness`, and `motion` live during the design session.

**Decision**: did not port the Tweaks panel host into the production Next.js page. Reasons:
1. The Tweaks panel is a design-session ergonomic, not a visitor feature. Eunomia ship criteria do not mention it. Designer Prompt 2 mandate is the visual + voice + interaction, not the runtime adjustability.
2. Porting it would pull a window.parent postMessage protocol into production, which serves no purpose without the design host parent.
3. The defaults are now baked: `warmth=55`, `doorStrength=78`, `motion=animated-city`. These can be exposed via future runtime control if needed.

**Risk to downstream**: low. The `EntryApp` component holds `warmth`, `doorStrength`, `motion` in `useState` already (initialized from `TWEAK_DEFAULTS`). Wiring runtime control later is a one-prop change.

**Mitigation**: documented in `EntryApp.tsx` header comment plus this journal. If panitia or Ghaisan asks for live tuning during demo, the path forward is to add the controls back as a Wave 2 polish task.

## Closing

No HIGH-bar ferry triggers. Three medium-confidence observations surfaced for Eunomia + V1 Orch awareness. Hestia ships per ship criteria, downstream sibling-worker fixes tracked in U2 do not block Hestia output integrity.
