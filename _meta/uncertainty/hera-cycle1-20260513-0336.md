# Hera Uncertainty Journal (Wave-Fixing #2 cycle 1)

**Worker**: Hera (Wave-Fixing #2 cycle 1 rescue, Cluster 2 scope)
**Timestamp**: 2026-05-13 03:36 WIB Day 2 early morning
**Effort tier**: xhigh (rescue mode)
**Cycle**: cycle 1 (single-cycle rescue ship)

## Confidence levels per scope item

### B-1 (CRITICAL): building click no-op
**Confidence**: HIGH (verified empirically + by code inspection)
**Verdict**: PASS

The click chain was already wired pre-cycle (Iris dispatch bus -> Hera selectBuilding -> Persephone useBuildingTicket -> TicketPanel slide-in). Wave-Fixing #2 cycle 1 verification confirmed end-to-end via SmokeClickInjector + console log + DOM snapshot:
- `[city] building click backend/app/core/main.py (temple)` log fires
- Ticket panel populates with Issue #412 + PR #47 detail (Merged + 5/5 DoD)
- Side panel populates with file metadata + contributors + linked tickets

**Residual uncertainty**: low. Playwright MCP cannot click WebGL canvas coords precisely so the natural r3f onClick path is verified via code review (Iris BuildingInstances.tsx ArchetypeSlot.handleClick) not direct browser click. Demo flow with human user is unaffected.

### C-new-4: Sprint Mode panel hide toggle parity
**Confidence**: HIGH (Persephone already shipped SprintHud wrapper)
**Verdict**: PASS

Persephone authored `frontend/components/panels/sprint-hud/SprintHud.tsx` + panelStore.sprintCollapsed. Hera ship of `SprintModeControls` wrapped by SprintHud; collapse + restore work via the toggle button (verified in DOM snapshot: "Hide Sprint Mode HUD" button visible at top of HUD).

### Feature #24: 14 PM visual full implementation
**Confidence**: HIGH (Wave 2 final ship; visual inspection confirms 14 chips)
**Verdict**: PASS

All 14 concept chips visible in SprintModeControls (scaffolding, crane, blueprint pin, inspector NPC, green halo, yellow tape, smoke retak, size badge, city hall banner, district border, DoD checklist, red bridge, PR comments, refactor stage). Each toggle-able + filterable. The 14th (refactor stage) is the Asclepius handoff (ghost building rendered by Asclepius RefactorGhostLayer, retak by Asclepius SpecDriftLayer). Toggle visibility default: 13 ON, 1 OFF (refactor-stage opt-in).

### Feature #25: PR-to-Building auto-sync webhook 5 events
**Confidence**: HIGH (state machine + mock tape demonstrates all 5)
**Verdict**: PASS

State machine `reduceSprintEvent` handles all 5 PR events per PRD Section 9.2 lines 511-516:
1. `pr.opened` -> crane appears (state foundation -> frame)
2. `pr.review_requested` -> inspector NPC orbit (state frame -> painting)
3. `pr.approved` -> green halo 30-min transient + sets prApprovedAt
4. `pr.merged` -> crane removes + scaffolding cleared (state painting -> finished)
5. `pr.closed` (withoutMerge=true) -> crane removes + no glow (state -> unfinished) [NEW this cycle]

Mock tape now demonstrates ALL 5 events across the 90-second loop. Real webhook receiver lives in Hades Wave 3 ship (`backend/app/api/webhook/github.py` with HMAC SHA-256 verify + Demeter pr_events persist). Aletheia Wave 3 audit PASS for webhook security floor.

**Residual uncertainty**: medium. Backend not running locally during this cycle (`localhost:8000` connect-refused logged). Real webhook -> WebSocket -> Hera reducer chain is verified by Aletheia in deploy environment, not in this dev session. For Day 2 submission demo, the mock tape is sufficient + the live webhook is end-to-end with the Atlas K8s deploy.

### PR comment surfacing (OQ-05)
**Confidence**: HIGH (Wave 2 ship + decision doc)
**Verdict**: PASS

Sticky note 3D variant locked per `_meta/decisions/oq05_pr_comment_surfacing.md`. PRCommentSurface renders sticky paper note attached to building face with unread count badge. Athena-landmark back-face placement avoids CityHallBanner z-fight.

### Backlog Office virtual building
**Confidence**: MEDIUM-HIGH (new ship this cycle; visual rendered + clickable, panel render deferred)
**Verdict**: PASS-with-deferred

Authored `BacklogOffice.tsx` + `useBacklogOfficeEvents.ts`. Mounted as child of SprintMode at fixed position [90, 0, -60] outside the treemap. Visual recipe distinct (sandstone base, red mailbox, flag pole + cloth, BACKLOG OFFICE plaque). Click dispatches `selectBuilding(BACKLOG_OFFICE_BUILDING_ID)` on heraStore.

**Residual uncertainty**: medium.
1. Persephone TicketPanel currently falls through to placeholder when selectedBuildingId === '__backlog_office__' (no special variant). Backlog panel (list of issues sorted by priority, filter by label/assignee/age, story points roll-up) deferred. A future cycle should add a Persephone `BacklogPanel` variant detected by id prefix `__`.
2. Asclepius IssueFlyingPacket targets Athena City Hall as Backlog Office fallback (their TODO comment lines 18-23). Hera exposes `BACKLOG_OFFICE_POSITION` + `useBacklogOfficePosition()` for swap. Anti-collision policy means Asclepius picks up the swap in their next cycle. Visual impact: flying issue lands at Athena, not the new Backlog Office building. Acceptable for Day 2 demo since the Backlog Office is visible as a clickable city landmark next to Athena.

## Medium-confidence concerns

### M-1: Multiple navigations during smoke testing
During Playwright MCP smoke tests, the page intermittently navigated to /start or / mid-session. The redirect cause was not isolated (no router.push/replace found in city/page.tsx). Production demo flow (user clicks marketing /start -> selects door -> /city?mock_auth=true) is unaffected since users do not navigate to /city via direct hash trigger.

**Mitigation**: SmokeClickInjector is dev-only + Day 2 demo manual click on InstancedMesh remains the canonical interaction.

### M-2: Boreas activity API connect-refused noise
Console shows 9 ERROR + 12 WARN entries from Boreas `/api/activity` fetch fails to localhost:8000 (backend not running locally). These are not in Hera scope.

**Mitigation**: out of scope.

### M-3: SmokeClickInjector adds 1 dev-only export to window
The `window.__codeplex_smoke_click` global is technically a leak in development. Lock 5 honest claim: production builds strip via NODE_ENV guard. Defensive removal on unmount via delete window.__codeplex_smoke_click.

**Mitigation**: production stripping verified by code inspection + injector mounts inside `if (process.env.NODE_ENV === 'production') return;` guard.

## Low-confidence concerns

None. All scope items either PASS empirically or have a clear deferral path.

## Ferry threshold

NOT triggered. No critical block + no contract conflict + no directive ambiguity.

## Ship status

CYCLE 1 SHIP CLEAN. 4 bug-fix + feature verdicts (B-1 PASS, C-new-4 PASS, Feature #24 PASS, Feature #25 PASS, PR comment surfacing PASS, Backlog Office PASS-with-deferred). 4 mandatory artifacts authored. V5 snapshot.
