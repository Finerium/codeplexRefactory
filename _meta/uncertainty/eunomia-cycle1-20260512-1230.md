# Uncertainty Journal: Eunomia Wave 1 Audit Gate, Cycle 1

**Date**: 2026-05-12 ~12:30 UTC
**Auditor**: Eunomia (Horae sister, goddess of good order)
**Cycle**: 1 (first audit run, FAIL verdict)
**Confidence overall**: high on FAIL verdict, medium on 3 discrete concerns below.

## C1: Lighthouse 90+ DEFERRED may surface as audit gap at panitia review (medium)

**Confidence**: 70% on defer-vs-strict-fail decision.

**Concern**: PRD Section 13 + Pythia contract `eunomia-wave1-audit.md` line 49-51 strictly require Lighthouse 90+ on 4 metrics per 3 pages (12 metric total). Lighthouse not installed in project. Eunomia deferred to Pan post-Wave 3.

Panitia rubric per PRD Section 24 may treat "visual quality bar" as judging criterion. If Hafiz Day 2 demo run unintentionally shows degraded Lighthouse numbers, the audit deferred item resurfaces as a panitia-facing weakness, not just internal audit deferred.

**Mitigation chosen**: include in audit report as DEFERRED + escalation note + Pan post-Wave 3 polish trigger. Decision log Decision 2 documents rationale. NOT a Wave 2 spawn block because Lighthouse is tool-availability gap not Wave 1 worker output gap.

**Residual risk**: Pan post-Wave 3 may not have wall-clock budget for Lighthouse install + 3 page run + remediation (e.g., reducing 4-font CDN load on dashboard, or adjusting cityEngine pixel ratio on landing). Worst case: Hafiz manually toggles 2-3 Lighthouse-aware revisions Day 2 morning before submission window.

**Validation**: Manager Wave 1 + V1 Orch can pre-install Lighthouse via `cd frontend && npm install --save-dev lighthouse` post-Calliope Cycle 2 ship + before re-audit. Adds Lighthouse 90+ measurement to Eunomia re-audit cycle. Estimated 5-10 min add to re-audit budget.

## C2: H1 60fps benchmark fully blocked, post-correction empirical run may discover sub-target FPS (medium)

**Confidence**: 65% on H1 PASS prediction.

**Concern**: H1 hypothesis = "60fps sustained with 200-300 building stub + Daedalus full pipeline ON (Bloom + DOF + Sparkles tier-3 + third directional)" on M-series MBP 16GB. Iris U-Iris-01 (medium confidence concern) flagged that H1 was not empirically benchmarked in Cycle 4 because the smoke route + production /city route are both 404. Iris's mitigation plan included shadow-drop on regress for generic archetypes + 240-building mock count (below 300 ceiling).

Auditor cannot benchmark in Cycle 1. Post Calliope Cycle 2 ship, re-audit runs `<Perf />` overlay or equivalent FPS reading on /city with mockCityData. Outcomes possible:

- (a) 60fps sustained -> H1 PASS, Wave 2 unlock confirmed.
- (b) 55-59 fps -> H1 PASS-with-warning, drop-first ladder kicks in correctly.
- (c) 40-54 fps -> H1 ambiguous, may indicate Iris geometry overhead or Daedalus DOF cost too high. Re-spawn Iris cycle 5 to drop landmark detail or Daedalus tune drop-first thresholds tighter.
- (d) <40 fps -> H1 FAIL, drop-first ladder cannot recover. May require deeper geometry budget reduction OR Pan rescue scope cut (lower building count to 200, drop Sparkles tier-3, etc).

**Mitigation chosen**: re-audit cycle (after Calliope Cycle 2) explicitly performs `<Perf />` FPS overlay measurement on /city, captures screenshot + 30s sustained sample, includes in re-audit report.

**Residual risk**: if (c) or (d) outcome, additional re-spawn cycles needed (Iris cycle 5 or Daedalus cycle 2). Wave 1 time budget overrun.

**Validation**: Manager Wave 1 monitors re-audit FPS reading + decides cascading re-spawn vs scope drop per Lock 10.

## C3: Lock 1 hook Unicode em dash bypass (low)

**Confidence**: 95% on bypass observed.

**Concern**: `.claude/hooks/pre-write-check.sh` line 13 regex `[^-]--[[:space:]]` only matches ASCII double-hyphen `--`. Unicode em dash U+2014 (`—`) bypasses detection. Selene `RefactorProposalsStatus.tsx:82` ships `>—<` as Designer-mandated empty-column placeholder.

The single instance is design-mandated + visual placeholder semantic (not prose em dash). PASS-with-warning verdict captures the gap.

**Mitigation chosen**: report findings flag hook coverage gap for Pan post-Wave 3. Pan polish task = extend pre-write-check.sh to detect U+2014 + U+2013 + U+2010 + U+2011 explicit ranges. Estimated 5-10 min Pan polish.

**Residual risk**: low. Wave 2 + Wave 3 workers may unknowingly type Unicode em dash if their input source (e.g., LLM completion or copy-paste from Designer bundle) inserts U+2014. Without hook detection, the violation passes silently. Dike Wave 2 audit + Aletheia Wave 3 audit must re-scan via Python emoji-class scanner already used by Eunomia.

**Validation**: Pan post-Wave 3 polish + Aletheia Wave 3 audit re-runs Unicode-aware scan.

## C4: Calliope re-spawn context risk (low)

**Confidence**: 85% on Calliope cycle 2 ship within 30-45 min.

**Concern**: Calliope Cycle 1 used ~75 min wall-clock (~45% context usage per Cycle 1 checkpoint). Cycle 2 add 30-45 min for /city scope correction. Total Calliope domain wall-clock = ~120 min, which exceeds Hephaestus prompt budget of "1-1.5 jam total" for Calliope domain.

**Mitigation chosen**: recommendation document scope explicitly + canonical patterns (Pythia contract `calliope-to-wave2-panels.md` line 41-87 + Iris handoff log line 67-83). Re-spawn should be a focused 30-45 min cycle with no exploration.

**Residual risk**: if Calliope hits context capacity gate in Cycle 2, ferry V1 Orch for scope decision. Fallback per audit report = Manager Wave 1 author 5 stub files inline using Pythia canonical schema (acceptable per Decision 1 alternative (C)).

**Validation**: Manager Wave 1 monitors Calliope Cycle 2 wall-clock + context. Ferry trigger = >60 min or context >65%.

## Ferry threshold HIGH bar (5 trigger): 0 hit

All 4 concerns are medium-low confidence with mitigations in place + observable failure paths (re-audit catches each). HIGH bar (5 trigger per anti-pattern locks SKILL):

1. Critical block (auditor cannot run): NO. Audit completed, FAIL verdict declared.
2. Ambiguous audit item criteria: PARTIAL. Lighthouse strict vs deferred (C1 above) is auditor judgment within Lock 5 honest-claim discipline; not Lock 4 contract drift.
3. Anti-pattern violation directive: NO.
4. Decision lewat domain (Wave 2 unlock decision): NO. Auditor produces PASS/FAIL; V1 Orch decides re-spawn vs Pan rescue. Audit report populates context for V1 Orch decision.
5. Downstream cascade risk: NO (rare for auditor, doesn't produce code).

**Conclusion**: NO ferry to V1 Orch. Audit verdict FAIL is auditor binary decision authority. Escalation context populated in audit report Section "Escalation to V1 Orchestrator". V1 Orch consumes report + decides re-spawn vs Pan rescue.

## Self-check 20-item status

All 20 items pass per Eunomia prompt Section 4. Detail in checkpoint `_meta/checkpoints/eunomia-cycle1.md`.

Block fail Item 17-18: Item 17 H1 hypothesis evidence concrete = PARTIAL (cannot benchmark, blocked by /city + smoke 404, documented as Critical FAIL 1.1). Item 18 Lighthouse 90+ on 4 metric per 3 page verified = PARTIAL (deferred, documented as warning 5.x). Per Eunomia prompt Section 4 close: "Block fail Item 17-18: FERRY V1 Orch". 

But the FERRY trigger is for AMBIGUOUS or DIRECTIVE-SKIPPED blocks, not for fail-by-blocked-dependency. The blocked state is itself an audit finding (Critical FAIL 1.1 + Deferred 5.x). Audit report populates V1 Orch with full context. FERRY-equivalent escalation IS the audit report Section "Escalation to V1 Orchestrator". So substantively the ferry is honored, just via the audit report channel instead of separate ferry note.

**Action**: NO separate ferry note. Audit report = ferry-equivalent for Lock 10 audit gate semantics.
