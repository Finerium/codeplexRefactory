# Pandora Checkpoint: Manager FINAL Cycle 2 (STAMP=20260513-0857)

[LOCK1_OVERRIDE: CLI flag documentation (curl flags + Docker COPY flags + git flags) intentionally retained for forensic traceability]

**Worker**: Pandora (Wave 3)
**Cluster**: D primary (Refactor URL-encoded fallback fix)
**Coordinated with**: Hades (Cluster A repo_root flow) + Asclepius (Cluster D frontend SSE consumer)
**Spawn time**: 2026-05-13 08:57 WIB Day 2 morning
**Ship time**: 2026-05-13 09:14 WIB (T+17 min, well under 25 min ferry threshold)
**Ship status**: PASS

---

## 20-item self-check

### Output completeness (5)

1. [x] Root cause identified: `has_openspec_folder(Path("."))` returns False inside runtime container because /app does NOT contain openspec/.
2. [x] Fix authored: 3-tier resolution chain (caller repo_root, bundled env+static hints, cwd walkup) in `github_issue_fallback.py`.
3. [x] Routes patched: both propose (SSE) + simulate endpoints use new `resolve_openspec_root` resolver.
4. [x] Dockerfile patched: COPY openspec into /app/openspec + .agent-openspec into /app/.agent-openspec after backend + frontend COPYs.
5. [x] Smoke tests authored: 6 pytest cases in `tests/test_openspec_detection_smoke.py`.

### Anti-pattern compliance (10)

6. [x] Lock 1 no em dash (verified across narrative text; only CLI flag literals retained under LOCK1_OVERRIDE header).
7. [x] Lock 2 no emoji (verified grep; ASCII only).
8. [x] Lock 3 drafts/ isolation safety property preserved (no simulation engine writes to production code; openspec/changes/ writes are per OpenSpec v1.0 dual-folder LOCKED PRD D27, NOT production code mutation).
9. [x] Lock 4 OpenSpec dual-folder Folder A + Folder B both generated (verified via OpenSpecGenerator.generate returning tuple).
10. [x] Lock 5 real-browser evidence: live curl SSE capture in `/tmp/pandora_stub_sse.log` (7884 bytes, 8 event types, 0 fallback events).
11. [x] Lock 6 no premature ship (verified pytest + live curl pass before deliverables authored).
12. [x] Lock 7 no contract drift (RefactorProposalEvent + GhostBuildingHint schemas unchanged; SSE event names unchanged).
13. [x] Lock 8 free registry only (Dockerfile COPYs add no new external dependencies).
14. [x] Lock 9 no hooks bypass (hooks intact during all writes).
15. [x] Lock 10 audit gate ready (Aether + Pan will consume the live curl evidence in `/tmp/pandora_stub_sse.log`).

### Contract integrity (3)

16. [x] RefactorProposalEvent schema unchanged: `proposal.complete` event still carries `simulationId`, `openspecChangePath`, `title`, `summary`, `userIntent`, `ghostBuildings`, `timestamp` per `_meta/contracts/pandora-to-asclepius.md`.
17. [x] drafts/ isolation verified: production code SHA-256 of `backend/app/main.py` + `frontend/src/modes/refactor/RefactorMode.tsx` unchanged (only `openspec/changes/<sim>/*.md` written, which is the OpenSpec change folder per spec, NOT production code).
18. [x] OpenSpec change folder generates clean output: `openspec/changes/add-2fa-to-login-013d07/proposal.md` + `design.md` + `tasks.md` exist with full templated content (proposal: 1303 bytes, design: 2300 bytes, tasks: 1848 bytes).

### Capacity + meta (2)

19. [x] Wall-clock under 25 min ferry threshold: T+17 min from spawn (08:57 to 09:14 WIB).
20. [x] 3 mandatory artifacts authored: this checkpoint, decision log append, handoff log.

---

## Live curl evidence (Lock 5 mandate)

Live test against fresh uvicorn with empty DEEPSEEK key forcing StubLLMClient deterministic, 30 sec budget:

```
EXIT=0
Total bytes: 7884
Event types fired (8):
  event proposal.queued
  event proposal.started
  event proposal.ghost
  event proposal.openspec.proposal_md
  event proposal.openspec.design_md
  event proposal.openspec.tasks_md
  event proposal.complete
  event proposal.simulate_ready

proposal.fallback.github_issue count: 0       (REGRESSION KILLED)
proposal.openspec.* count: 3                  (proposal/design/tasks streamed)
proposal.ghost count: 1                       (ghost building hint for Asclepius)
proposal.complete count: 1                    (final envelope with ghostBuildings array)
proposal.simulate_ready count: 1              (Run Simulation button enabled)
```

Full log: `/tmp/pandora_stub_sse.log`

---

## Files changed

1. `backend/app/services/refactor/github_issue_fallback.py`
   - Added `resolve_openspec_root(repo_root)` returning the Path under which openspec/ lives, or None.
   - Added `_bundled_openspec_root()` resolving BUNDLED_OPENSPEC_ROOT env + static hints (/app, /app/backend/..).
   - Added `_walkup_for_openspec(start)` walking up to 4 parents looking for openspec/.
   - Extended `has_openspec_folder(repo_root)` to also consult the bundled fallback chain.
2. `backend/app/api/refactor/routes.py`
   - Imports `resolve_openspec_root` alongside `has_openspec_folder` + `draft_github_issue`.
   - Propose endpoint (SSE branch): `resolved_openspec_root = resolve_openspec_root(repo_path)`, then `if resolved_openspec_root is not None: OpenSpecGenerator(repo_root=resolved_openspec_root)`; else GitHub Issue fallback.
   - Simulate endpoint: same pattern.
   - Both branches log the resolved root vs fallback decision for Aletheia audit.
3. `infra/docker/Dockerfile`
   - Added COPY directive to bundle openspec/ into /app/openspec after frontend public COPY.
   - Added COPY directive to bundle .agent-openspec/ into /app/.agent-openspec.
   - Comment block referencing the Cluster D fix + detection chain.
4. `backend/tests/test_openspec_detection_smoke.py` (NEW)
   - 6 pytest cases covering the resolution chain.
   - All 6 PASS in 0.19 sec.

---

## Coordination with other clusters

- **Cluster A Hades**: when Hades ships the `/api/repos/list` then clone-to-tmp flow, the cloned repo path becomes the `repo_root` query arg the frontend passes to `/api/refactor/propose`. With this fix, Hades-cloned repos that DO have openspec/ light up generator-into-clone-path; repos WITHOUT openspec/ fall through to bundled (vs URL-encoded link). No Hades API change required.
- **Cluster D Asclepius (frontend SSE consumer)**: SSE event names + payload shapes unchanged. Asclepius's `streamProposal` AsyncIterable consumer continues working without modification. The frontend will now receive `proposal.openspec.proposal_md` + `design_md` + `tasks_md` events with full markdown body for the side panel to render chunk-by-chunk, plus the existing `proposal.ghost` events for 3D ghost building rendering, plus the `proposal.complete.ghostBuildings` array as the canonical envelope.
- **Cluster F Health/Nemesis verify-real-data**: unrelated; no overlap.

---

## Ferry status

NOT triggered. Wall-clock under 25 min, no contract conflicts, no anti-pattern violations, no down-stream cascade.
