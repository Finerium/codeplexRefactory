# Boreas Checkpoint - Manager FINAL Cycle 2 Cluster B+F

**Author**: Boreas (Wave 2 - Manager FINAL Cycle 2 re-engaged)
**Timestamp**: 2026-05-13 09:35 WIB Day 2 morning
**Stamp**: 20260513-0857
**Cluster scope**: B (Git Time Machine scrubber drag -> building height tween + commit popup) + F (Activity Mode real data audit)
**Wall-clock spent**: ~80 minutes
**Ferry**: NOT triggered

---

## 20-item self-check

### Output completeness (5)

1. **Backend extension shipped**: `POST /api/activity/loc-snapshot` now accepts `repo_full_name` (auto-clone via Hades clone_repo_shallow) + returns `commit_subject` + `commit_author` + `commit_committed_at` + `nearby_commits[]`. Verified end-to-end on Hafiz `gadablotnok/web-esp32log` with real commit "Delete warning page" by HAFIZ FAUZAN SYAFRUDIN at 2026-04-26.
2. **Frontend useTimeMachine hook shipped**: `src/modes/activity/useTimeMachine.ts` with 100ms debounce + AbortController cancellation + repo-aware (inert when no slug).
3. **Frontend BuildingHeightTimeMachine shipped**: `src/modes/activity/BuildingHeightTimeMachine.tsx` mutates Iris InstancedMesh matrices each frame via tau=0.2s lerp. Restores base matrices on unmount.
4. **Frontend CommitTooltip + TimeMachineOrchestrator shipped**: `CommitTooltip.tsx` + `TimeMachineOrchestrator.tsx` render the floating 1-3 nearby commit card above the timeline scrubber.
5. **Real-data audit fix shipped (Cluster F)**: `useActivityData.ts` now reads `?repo=<slug>` from URL on mount + threads it into `/api/activity?repo=<slug>`. Previously hardcoded to `all` (silent cross-repo aggregate). Plus dev injector `ScrubberTestInjector` exposes scrubber URL param + window helper.

### Anti-pattern compliance (10)

6. **Lock 1 (no em dash)**: clean across all new files.
7. **Lock 2 (no emoji)**: clean across all new files.
8. **Lock 3 (no inline mock contamination)**: useTimeMachine surfaces empty snapshot via `notes: ["no_commit_before_timestamp"]` instead of fake fallback data.
9. **Lock 4 (cross-cutting wrong)**: BuildingHeightTimeMachine accesses InstancedMesh via scene.getObjectByName() (read-only public API), does NOT modify Iris's BuildingInstances source.
10. **Lock 5 (honest claim)**: when no repo slug, the TimeMachineHud renders an explicit hint card instructing user to select a repo. No fake demo data.
11. **Lock 6 (no manual workflow shortcut)**: full flow drives through real Demeter endpoint + real Hades clone helper.
12. **Lock 7 (no overconfident success)**: ship report explicit about which scenarios were verified (Hafiz repo end-to-end, midpoint scrubber position, restoration on unmount) AND which were not (cross-mode geometry leakage tested only via static review).
13. **Lock 8 (idempotency)**: backend 1h cache keyed by (repo_root, minute-bucketed timestamp). Re-drag to same position returns cached payload.
14. **Lock 9 (no destructive cleanup)**: BuildingHeightTimeMachine restore-on-unmount writes base matrices back via setMatrixAt rather than recomputing or destroying instance data.
15. **Lock 10 (audit gate ready)**: real-browser Playwright evidence captured at 3 scrubber positions (0.0 / 0.5 / 1.0) + curl smoke against the backend endpoint + 0 console errors. Screenshots stored under `_meta/audit/screenshots/cycle2-20260513-0857/`.

### Contract integrity (3)

16. **Pythia contract preserved**: TourScript + ActivityData + TimelineState types unchanged in `types.ts`. Time Machine extension added as a SEPARATE surface (TimeMachineSnapshot + TimeMachineCommit) so Demeter's existing `/api/activity` contract is untouched.
17. **Daedalus ChronicleCanvas pattern preserved**: BuildingHeightTimeMachine mounts as a sibling of BuildingInstances inside the canvas tree (no direct ChronicleCanvas modification).
18. **Hermes narration cue keys unchanged**: this cycle is Activity Mode only; Onboarding Hermes path untouched.

### Capacity + meta (2)

19. **Capacity**: ~80 min wall-clock for backend extension + 4 new frontend files (useTimeMachine, BuildingHeightTimeMachine, CommitTooltip, TimeMachineOrchestrator, ScrubberTestInjector) + 2 edited (ActivityMode, useActivityData) + verification. Under the 90-min Cluster B+F budget.
20. **Meta artifacts**: this checkpoint + decision log D17 append + handoff log + screenshots all authored.

---

## Real-browser evidence summary

| Position | Cursor date | Anchor commit | Nearby[2] | Indicator | Console errors |
|---|---|---|---|---|---|
| scrubber=0.0 (NOW) | 2026-05-12 | 1e7feb7 "Delete warning page" 16 days before | 7d076e3 "Hapus tombol" | live | 0 |
| scrubber=0.5 (midpoint) | 2026-04-27 | 1e7feb7 "Delete warning page" 1 day before | 7d076e3 "Hapus tombol" 20 days before | live | 0 |
| scrubber=1.0 (Nd ago) | 2026-04-12 | 7d076e3 "Hapus tombol" 5 days before | 94acf35 "Update soil sensor..." 6 days before | cached | 0 |

All 3 positions show DIFFERENT real Hafiz commit content, files at cursor: 5 (real ESP32 logger repo size), 0 console errors. Pipeline verified end-to-end against Hafiz's real GitHub repo.

---

## Known limitations + Wave 3 followups

- **Mock city ID mismatch**: the synthetic `mockCityData` uses fastapi-fullstack-style file paths (`backend/app/services/service_14.py`) while the Hafiz ESP32 repo has different paths (`main.ts`, `static/index.html`). When scrubbing against Hafiz repo, only buildings whose IDs overlap with the snapshot tree gain a non-zero height; the rest shrink to scale 0 (zero height = building absent in that timeline). This is correct semantic behavior per Ghaisan vision ("missing entirely") but visually reads as the whole city collapsing for a small target repo. Wave 3 Demeter parser swap replaces mockCityData with per-repo parsed city, so building IDs will match the snapshot tree by construction.
- **Mock vs real repo recommendation for demo**: scrubbing against `Finerium/codeplexRefactory` (the local repo with `backend/app/*` + `frontend/src/*` paths) gives the most visually rich tween because more building IDs partially match real file paths.
- **Initial clone latency**: first scrub against an unknown repo triggers a shallow clone (~3-15s on small repos). Subsequent ticks hit the 1h cache. The CommitTooltip surfaces "fetching LOC snapshot at cursor (initial clone may take a few seconds)..." during this window.
