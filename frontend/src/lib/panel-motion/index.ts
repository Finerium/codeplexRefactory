/**
 * Public barrel for panel motion utilities.
 *
 * Authored by Persephone (Wave 2). Re-exports GSAP-driven hooks.
 *
 * Compliance:
 *   Lock 1 (no em dash): clean.
 *   Lock 2 (no emoji): clean.
 */

export {
  useSlideTransition,
  type SlideDirection,
} from './useSlideTransition';
